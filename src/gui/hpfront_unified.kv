#:import UnifiedHPManager src.gui.unified.unified_hp_manager.UnifiedHPManager

<HpFront>:
    orientation: 'vertical'

    # Replace the entire TabbedPanel with UnifiedHPManager
    UnifiedHPManager:
        id: unified_hp_manager
        size_hint_y: 1.0  # Take the full height
