#:import HPManager src.gui.hp_manager.hp_manager.HPManager

<HpFront>:
    orientation: 'vertical'

    # Replace the entire TabbedPanel with HPManager
    HPManager:
        id: hp_manager
        size_hint_y: 1.0  # Take the full height
