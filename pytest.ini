[pytest]
log_cli = true
log_format = %(asctime)s  %(name)s  %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli_level = INFO

filterwarnings =
    ignore::FutureWarning

asyncio_mode = auto